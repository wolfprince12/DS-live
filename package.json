{
  "name": "dsondt",
  "private": true,
  "version": "0.4.1",
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
